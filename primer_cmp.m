function bool_cmp = primer_cmp(primers1, primers2, opts)
% Compare 2 sets of primers, given options for prioritiztion of features.
% Return a boolean value: 'true' if primers1 is better than primers2,
% 'false' otherwise.
%
% This function is to be used if we decide that we cannot come up with a
% function to use to score primers; doubtless numbers are cheaper to cmp
% than primers.
end
